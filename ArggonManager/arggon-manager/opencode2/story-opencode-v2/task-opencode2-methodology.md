---
type: task
status: done
id: task-opencode2-methodology
title: Methodology commands and skill progressive disclosure
assignee: Arggon
branch: feat/task-opencode2-methodology
parent: story-opencode-v2
labels: []
priority: p2
created: "2026-09-18"
updated: "2026-09-18"
depends_on: [task-opencode-v2-spec]
---
<!--
  Placement (v0): tasks/arggon-manager/opencode2/story-opencode-v2/task-opencode2-methodology.md
  Leaves live only under a story. id is the filename stem: task-opencode2-methodology.
  parent MUST be the story id. Omit assignee when unassigned. Omit blocked_reason unless status is blocked.
-->

# Methodology commands and skill progressive disclosure

## Context

W5 of [plan-opencode2-009](../../../docs/plans/plan-opencode2-009.md): the
methodology pipeline (explorations, ADRs, specs, plans, playbooks) becomes
native V2 workflow entry points, and the monolithic `arggon-cli` skill
(313 lines, everything in the body) is refactored for progressive disclosure so
the model loads detail on demand instead of carrying it on every prompt.

## Acceptance

- [x] Commands generated by `init` (or documented from the seam spec):
      `/arggon-spec`, `/arggon-adr`, `/arggon-explore`, `/arggon-playbook`,
      each a Markdown prompt template that drives the existing CLI scaffolds
      (`spec new`, `stack explore`, `playbook new`) plus `spec validate` /
      `spec analyze` verification; no shell blocks with argument placeholders.
- [x] Skill refactor: `skills/arggon-cli/SKILL.md` keeps the umbrella (when to
      use, install, work loop) and moves operational detail into
      `references/` files (json contract, methodology, orchestration, pitfalls);
      V2 lists supporting files and the agent reads them on demand.
- [x] Source/bundle parity: `skill-copy.test.ts` and `skills:sync` stay green;
      reference files are bundled and drift-tested like `SKILL.md`.
- [x] Evidence: a real session shows the reference being read on demand (not on
      load), and `/arggon-spec` scaffolds + validates a spec on a fixture.
- [x] Docs travel: `docs/agents.md` §Reference integrations and the playbook
      updated in the same PR.

## Notes

- Depends on task-opencode-v2-spec for the generation contract; the skill split
  itself is independent of the plugin.
- Keep the advertised skill description short and stable — it is what the model
  sees per step.

### 2026-09-18 @Arggon
W5 implementation evidence (PR #328, no merge/status change — worker scope).

Commands: templates/docs/opencode/commands/arggon-{spec,adr,explore,playbook}.md generated by init; markers '---\n# arggon:generated template="opencode/commands/arggon-*.md"'; no !`...` shell blocks; ADR command points at docs/engineering.md §ADR process.

Skill: skills/arggon-cli/SKILL.md → umbrella (short stable description 0.4.0) + references/{json-contract,methodology,orchestration,pitfalls}.md; BUNDLED_SKILLS (cli/src/docs.ts) bundles them; init copies marker+x-generated per file; skill-copy.test.ts parity generalized + references-bleed test; skills:sync syncs 6 files; .agents/skills/ ignored wholesale.

Fixture /tmp/opencode/arggon-w5-evidence (init --json): created the 4 commands + 4 references; x-generated template+checksum per dest; bundle == marker+source byte-equal (5x OK); spec validate re-run ok:true.

Real opencode run sessions (opencode 2.0.7, model opencode-go/deepseek-v4-flash, --standalone --format json):
1) skill-on-demand: skill tool call {id: argon-cli} completed → read of .agents/skills/arggon-cli/references/methodology.md (not in the umbrella) → answer quotes it verbatim.
2) /arggon-spec: model read .opencode/commands/arggon-spec.md, ran 'arggon spec new board-since-flag --title "board --since flag" --plan --json' → files spec-board-since-flag-001.md + plan-board-since-flag-001.md; 'arggon spec validate --json' → errors:[],warnings:[]; one analyze finding (vague 'fast') resolved by edit, re-run clean.

Gates: npm test 1112 passed (69 files); lint+build clean; arggon validate ok; spec validate ok.

Ownership exceptions (needed for green suite): cli/src/init.test.ts TIER1_DOCS and cli/src/doctor.test.ts commands list/'seam 14' — both mechanical, no active worker owns them. Playbook NOT edited per brief: suggested docs/playbooks/opencode.md lines 60-61 (umbrella references on demand) and 84-88 (methodology commands); README init notes still list 6 commands/single-file skill (out of scope).

### handoff 2026-09-18 @Arggon — next: Review PR #328 (draft, base opencode2) — seam tests, skill split, evidence in the item comments. If it passes: merge (merge-commit; tracker auto-commits ride the branch), verify, then flip item done.
- branch: feat/task-opencode2-methodology
- open questions: Docs to update in W7 dogfood (reported, not edited): docs/playbooks/opencode.md lines 60-61 + 84-88, README init notes. W5 touched cli/src/init.test.ts and cli/src/doctor.test.ts mechanically to keep…

### 2026-09-18 @Arggon
Coordinator merge verification: review verdict NO-MERGE→fixed (docs travel: playbook/README/json-output updated in 95d06b2); implementation verified independently (10 commands, 4 references byte-parity, provenance, real session reading references on demand, 1112 tests); merged with cli pass. Closing.
