# Work tracking in {{PROJECT_NAME}}

This project tracks work **in-tree** under `ArggonManager/` as Markdown work items (initiatives, epics, stories, tasks, bugs) managed by [`arggon`](https://github.com/Arggon/ArggonManager). GitHub is used for **PRs only** — please do not open GitHub issues.

- Find work: `arggon list --status todo --json`
- File new work: `arggon create task|bug "<title>" --parent <story-id>`
- The layout and frontmatter schema live in `ArggonManager/.convention.yml` and, if present, [`ArggonManager/docs/convention.md`](convention.md).

Generated by `arggon init` {{YEAR}} — edit freely; `arggon init` never overwrites existing files.
