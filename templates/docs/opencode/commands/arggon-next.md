---
description: Show the next claimable item and how to claim it
---

Use the `arggon_next` MCP tool (or `arggon next --json`) to find the next
claimable item. $ARGUMENTS

Report, bounded: item id, title, priority, why it ranks first, its parent chain,
blocked-by/unblocks counts, and the exact claim command. Do not claim anything
without being asked.
