node_modules/
dist/
coverage/
.DS_Store
*.log

# task-adr0006-docs-budget: generated copies of skills/arggon-cli/** under
# .agents/skills/ (single committed source). task-opencode2-methodology (W5):
# the umbrella's references/ are bundled copies too. Recreate with
# `npm run skills:sync`.
.agents/skills/

# task-opencode-v2-plugin (W2): generated copy of
# opencode/plugins/arggon/index.ts (single committed source). Recreated by
# `arggon init` or the byte-parity test (cli/src/plugin-copy.test.ts).
.opencode/plugins/arggon/index.ts

# task-native-tui (W5): the TUI entry is vendored verbatim from
# opencode/plugins/arggon/tui.tsx (single committed source); `arggon init`
# recreates the copy. The board surface itself is inlined in the bundle above.
.opencode/plugins/arggon/tui.tsx

# Tooling artifacts (video2code / video-agent sessions)
.v2c/
.video_agent/
