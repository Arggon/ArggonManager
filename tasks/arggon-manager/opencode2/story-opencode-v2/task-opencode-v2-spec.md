---
type: task
status: in_progress
id: task-opencode-v2-spec
title: "Spec and plan: generated .opencode seam (config, agents, commands)"
assignee: Arggon
priority: p1
depends_on: [task-opencode-v2-adr]
parent: story-opencode-v2
branch: feat/task-opencode-v2-spec
worktree_path: /home/arggon/Projects/ArggonManager-opencode2-task-opencode-v2-spec
labels: []
created: "2026-09-18"
updated: "2026-09-18"
claimed_at: "2026-09-18T00:36:59.370Z"
---
<!--
  Placement (v0): tasks/arggon-manager/opencode2/story-opencode-v2/task-opencode-v2-spec.md
  Leaves live only under a story. id is the filename stem: task-opencode-v2-spec.
  parent MUST be the story id. Omit assignee when unassigned. Omit blocked_reason unless status is blocked.
-->

# Spec and plan: generated .opencode seam (config, agents, commands)

## Context

W1 of [plan-opencode2-009](../../../../docs/plans/plan-opencode2-009.md)
(T2 + T3): the declarative OpenCode V2 seam. Per
[ADR 0010](../../../../docs/adr/0010-opencode2-native-architecture.md) the seam
is generated by `arggon init`, zero-config for the adopter, never overwriting,
and free of rule duplication (prompts and permissions only, pointing at
`docs/agents.md` / `docs/engineering.md` / the `arggon-cli` skill).

## Acceptance

- [x] Spec `docs/specs/spec-opencode-seam-010.md`: purpose, invariants I1–I6,
      synopsis (template → destination table, marker styles, buckets),
      acceptance criteria; validates clean.
- [x] Invariants explicit: config generated only when the adopter has no
      OpenCode config (root or `.opencode/`); never-overwrite via the existing
      provenance machinery; JSON/JSONC valid without an HTML marker;
      frontmatter-first for agents/commands with the marker as a YAML comment.
- [x] Agent design mapped to the repo orchestration: coordinator (primary,
      subagent allow-list worker/reviewer/explore), worker (subagent, nesting
      denied), reviewer (subagent, `edit` denied).
- [x] Command design: `/arggon-next|start|done|handoff|status` as prompt
      templates driving MCP tools plus `/arggon-review`
      (`agent: arggon-reviewer`, `subagent: true`); no shell blocks with
      argument placeholders.
- [x] Plan `docs/plans/plan-opencode-seam-010.md` with ordered tasks T1–T5 and
      verifiable acceptance.
- [x] Spec and plan statuses flipped to `implemented` in this landing PR.

## Notes

**Shipped (landing PR on `feat/task-opencode-v2-spec` → `opencode2`):**

- 10 tier-1 templates under `templates/docs/`: `opencode.jsonc`,
  `.opencode/agents/arggon-{coordinator,worker,reviewer}.md`,
  `.opencode/commands/arggon-{next,start,done,handoff,review,status}.md`.
- `cli/src/docs.ts`: prefix path mapping (`opencode/` → `.opencode/`), the
  conditional config decision (`present-skip`, signature-based so an adopter
  config at any of the four shapes always wins), `stampGeneratedContent`
  (HTML marker / YAML-in-frontmatter / none for JSON/JSONC) shared by the plan
  and doctor's render path, `findOpenCodeConfig`.
- Tests: new `cli/src/init-opencode.test.ts` (creation, JSONC validity, marker
  shape, conditional skip for every adopter-config shape, config reappears when
  the adopter config disappears, provenance round-trip); counts in
  `init-docs.test.ts` / `init.test.ts` / `doctor.test.ts` made derived instead
  of hardcoded.
- Docs: README tier-1 list + marker rules; `docs/agents.md` §MCP server notes
  the V2 seam.

**Smoke evidence (fixture probes, 2026-09-18):**

- Fresh init: `created` 31 entries, the 10 seam destinations among them.
- Second run: `updated: 22, modified: 0, skipped: []` (tier-1 + skills).
- Adopter added `opencode.json` and deleted the generated file: no
  `opencode.jsonc` written; `skipped` contains it.
- Worker agent frontmatter:
  `---` / `# arggon:generated template="opencode/agents/arggon-worker.md"` /
  `mode: subagent`.
- Full suite: **1056 tests / 67 files green**; `arggon validate` and
  `spec validate` clean.

**Next:** W2 (task-opencode-v2-plugin) consumes this seam as the integration
base; the plugin bundles into `.opencode/plugins/arggon/` with the same
provenance + parity discipline.
