---
type: task
status: in_progress
id: task-native-layout-decision
title: "Layout decision: ArggonManager/ root + docs inside"
assignee: Arggon
branch: feat/task-native-layout-decision
parent: native-redesign
labels: []
priority: p0
created: "2026-09-19"
updated: "2026-09-19"
claimed_at: "2026-09-19T23:55:07.852Z"
worktree_path: /home/arggon/Projects/ArggonManager-opencode2-task-native-layout-decision
---
<!--
  Placement (v0): tasks/arggon-manager/opencode2-native/native-redesign/task-native-layout-decision.md
  Leaves live only under a story. id is the filename stem: task-native-layout-decision.
  CLI `arggon create task native-layout-decision` adds the task- prefix (do not pass it twice).
  parent MUST be the story id. Omit assignee when unassigned. Omit blocked_reason unless status is blocked.
-->

# Layout decision: ArggonManager/ root + docs inside

## Context

Product-owner directive (2026-09-19), amending ADR 0011 on confirmation:

- The tracker root is renamed `tasks/` → `ArggonManager/`.
- **All product docs** (adr, specs, plans, explorations, playbooks,
  convention/engineering docs, and the docs generated by `init`) live under
  `ArggonManager/docs/`.
- Legacy `tasks/` trees are **auto-detected and migrated** (no hard break).

This item records the decision (ADR 0012), flips ADR 0011 to Accepted with the
cross-reference, updates the native-first spec/plan to the new layout, and
files the migration wave.

## Acceptance

- [ ] `docs/adr/0012-*.md` merged (Accepted) recording the layout, docs
      placement, and legacy auto-detection + migration.
- [ ] ADR 0011 flipped to Accepted and cross-referencing 0012.
- [ ] `spec-native-first-011` and `plan-native-first-011` updated to
      `ArggonManager/` + `ArggonManager/docs/` with the migration requirement.
- [ ] `task-native-layout-rename` filed and W1 (`task-native-kernel-lib`)
      depends on it.
- [ ] `arggon validate` and `arggon spec validate` green; docs-only diff.

## Notes

- The migration itself (kernel, convention, this repo's dogfood rename) is
  `task-native-layout-rename`, not this item.
