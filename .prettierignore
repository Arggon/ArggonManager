# Authored plugin entry source: deliberately written semicolon-free since its
# first commit, against .prettierrc.json's defaults, so `npm run format` would
# rewrite the whole file (~1.5k changed lines) as style-only churn and bury its
# history. Policy (task-plugin-source-prettier-policy): keep the authored
# style; the generated bundle is what adopters receive, so formatting this
# source buys nothing. Do not reformat — the generated bundle stays pinned by
# `npm run check:plugin`.
opencode/plugins/arggon/

# Generated plugin bundle: rebuilt by `npm run build:plugin` and pinned
# byte-for-byte by cli/src/plugin-copy.test.ts / bundle.test.ts. Formatting it
# would only create drift.
opencode/plugins/arggon/index.bundle.ts

# Derived, gitignored copies (regenerated by `arggon init` / parity tests).
.opencode/
.agents/skills/
